name = "loversite-expiry-cron"
main = "index.js"
compatibility_date = "2024-11-01"

# Runs once a day at 02:00 UTC. Change the cron expression to suit your timezone.
[triggers]
crons = ["0 2 * * *"]

# Set with: wrangler secret put SUPABASE_URL
# Set with: wrangler secret put SUPABASE_SERVICE_KEY
